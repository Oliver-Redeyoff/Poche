{
  "name": "poche-browser-extension",
  "version": "1.0.0",
  "description": "Poche - Read it later browser extension",
  "type": "module",
  "scripts": {
    "dev": "vite build --watch --mode development",
    "build": "vite build",
    "build:chrome": "npm run build && rm -rf chrome && cp -r dist chrome",
    "build:firefox": "vite build --config vite.firefox.config.ts && cp manifest.firefox.json firefox/manifest.json && cp -r icons firefox/icons",
    "build:safari": "npm run build && cp -r dist safari",
    "icons": "node scripts/generate-icons.js"
  },
  "dependencies": {
    "@poche/shared": "file:../shared",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.0.0-beta.28",
    "@types/chrome": "^0.0.268",
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "@vitejs/plugin-react": "^4.3.3",
    "typescript": "^5.3.0",
    "vite": "^5.4.10"
  }
}
